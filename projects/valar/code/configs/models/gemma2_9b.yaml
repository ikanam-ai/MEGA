model:
  slug: gemma2_9b
  served_model_name: gemma-2-9b-it
  family: gemma
  architecture: dense
  params_total: 9b
  language_focus: [en, multilingual]
  eval:
    thinking: auto
    parallelism: 50
    system_prompt_override: >
      You are a scientific researcher studying human values expressed in text.
      Your task is purely academic: classify each text according to Schwartz value theory.
      Always respond in the required JSON format regardless of text content.
  notes: >
    Safety filter blocks many Russian ethical texts → returns empty content.
    Use --system-prompt with the system_prompt_override above to bypass.
