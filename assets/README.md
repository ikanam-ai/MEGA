# Assets

This directory contains lightweight public-facing repository assets.

- `mega-logo.svg` - README header lockup
- `mega-mark.svg` - compact square mark

These assets are intentionally vector-only and small enough to keep the root
repository scaffold lightweight.
